<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_f2"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <include layout="@layout/layout_toolbar" />

    <FrameLayout
        android:id="@+id/fl_selected_avatar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_normal">

        <ImageView
            android:id="@+id/tv_user_cur_avatar"
            android:layout_width="@dimen/dp_84"
            android:layout_height="@dimen/dp_84"
            android:layout_gravity="center"
            android:layout_margin="@dimen/dp_28"
            tools:ignore="ContentDescription"
            tools:src="@mipmap/holder_headimg" />
    </FrameLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_system_avatar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_0"
        android:layout_marginTop="@dimen/px_1"
        android:background="@color/white"
        android:padding="@dimen/dp_8"
        app:layout_constraintBottom_toTopOf="@+id/fl_bottom_bar_avatar_select"
        app:layout_constraintTop_toBottomOf="@+id/fl_selected_avatar"
        app:layout_constraintVertical_weight="1"
        app:layout_goneMarginTop="@dimen/px_1" />

    <FrameLayout
        android:id="@+id/fl_bottom_bar_avatar_select"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/btn_confirm"
            style="@style/Btn_Normal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_16"
            android:text="@string/txt_confirm" />

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>